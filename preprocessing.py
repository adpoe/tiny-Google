""" File used to pre-process our data.
        - Goal is to add the filename to start of each line, separated by a \t.
        - This will make parsing input and adding document id's simpler
"""

import os
import sys

